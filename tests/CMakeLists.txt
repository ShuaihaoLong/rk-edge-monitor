add_executable(rkmon_core_tests core_tests.cpp)
target_link_libraries(rkmon_core_tests PRIVATE rkmon::core rkmon::app)
target_compile_options(rkmon_core_tests PRIVATE -Wall -Wextra -Wpedantic)
if(NOT CMAKE_CROSSCOMPILING)
    foreach(suite queue mailbox service logger application)
        add_test(NAME core_${suite} COMMAND rkmon_core_tests ${suite})
        set_tests_properties(core_${suite} PROPERTIES TIMEOUT 15)
    endforeach()
endif()

add_executable(rkmon_stm32_tests stm32_tests.cpp)
target_link_libraries(rkmon_stm32_tests PRIVATE rkmon::stm32)
target_include_directories(rkmon_stm32_tests PRIVATE "${PROJECT_SOURCE_DIR}/src")
if(NOT CMAKE_CROSSCOMPILING)
    add_test(NAME stm32_protocol COMMAND rkmon_stm32_tests)
endif()

add_executable(rkmon_camera_tests camera_tests.cpp)
target_link_libraries(rkmon_camera_tests PRIVATE rkmon::camera)
target_compile_options(rkmon_camera_tests PRIVATE -Wall -Wextra -Wpedantic)
add_executable(rkmon_camera_probe camera_probe.cpp)
target_link_libraries(rkmon_camera_probe PRIVATE rkmon::app)
target_compile_options(rkmon_camera_probe PRIVATE -Wall -Wextra -Wpedantic)
if(NOT CMAKE_CROSSCOMPILING)
    add_test(NAME camera_service COMMAND rkmon_camera_tests)
    set_tests_properties(camera_service PROPERTIES TIMEOUT 15)
endif()

add_executable(rkmon_config_tests config_tests.cpp)
target_link_libraries(rkmon_config_tests PRIVATE rkmon::app)
add_executable(rkmon_app_factory_tests app_factory_tests.cpp)
target_link_libraries(rkmon_app_factory_tests PRIVATE rkmon::app)
if(NOT CMAKE_CROSSCOMPILING)
    add_test(NAME app_config COMMAND rkmon_config_tests)
    add_test(NAME app_factory COMMAND rkmon_app_factory_tests)
    set_tests_properties(app_config app_factory PROPERTIES TIMEOUT 15)
endif()
if(NOT CMAKE_CROSSCOMPILING)
    find_package(Python3 REQUIRED COMPONENTS Interpreter)
    add_test(NAME stm32_integration COMMAND "${Python3_EXECUTABLE}"
        "${CMAKE_CURRENT_SOURCE_DIR}/stm32_integration_tests.py" "$<TARGET_FILE:rkmon>")
    set_tests_properties(stm32_integration PROPERTIES TIMEOUT 30)
    add_test(NAME app_startup COMMAND "${Python3_EXECUTABLE}"
        "${CMAKE_CURRENT_SOURCE_DIR}/startup_tests.py" "$<TARGET_FILE:rkmon>"
        "${PROJECT_SOURCE_DIR}/script/start.sh")
    set_tests_properties(app_startup PROPERTIES TIMEOUT 20)
endif()

add_executable(rkmon_video_tests video_tests.cpp)
target_link_libraries(rkmon_video_tests PRIVATE rkmon::video)
target_include_directories(rkmon_video_tests PRIVATE "${PROJECT_SOURCE_DIR}/src")
add_executable(rkmon_video_probe video_probe.cpp)
target_link_libraries(rkmon_video_probe PRIVATE rkmon::app)
target_include_directories(rkmon_video_probe PRIVATE "${PROJECT_SOURCE_DIR}/src")
if(NOT CMAKE_CROSSCOMPILING)
    add_test(NAME video_service COMMAND rkmon_video_tests)
    set_tests_properties(video_service PROPERTIES TIMEOUT 15)
endif()
if(RKMON_WITH_GSTREAMER)
    # 需真实 MPP 插件，作为板端显式执行项，不加入主机 CTest。
    add_executable(rkmon_gst_decoder_tests gst_decoder_tests.cpp)
    target_link_libraries(rkmon_gst_decoder_tests PRIVATE rkmon::video PkgConfig::GST)
    target_include_directories(rkmon_gst_decoder_tests PRIVATE "${PROJECT_SOURCE_DIR}/src")
endif()

add_executable(rkmon_stream_tests stream_tests.cpp)
target_link_libraries(rkmon_stream_tests PRIVATE rkmon::video)
target_include_directories(rkmon_stream_tests PRIVATE "${PROJECT_SOURCE_DIR}/src")
if(NOT CMAKE_CROSSCOMPILING)
    add_test(NAME stream_service COMMAND rkmon_stream_tests)
    set_tests_properties(stream_service PROPERTIES TIMEOUT 15)
endif()

add_executable(rkmon_ai_tests ai_tests.cpp)
target_link_libraries(rkmon_ai_tests PRIVATE rkmon::ai)
target_include_directories(rkmon_ai_tests PRIVATE "${PROJECT_SOURCE_DIR}/src")
if(NOT CMAKE_CROSSCOMPILING)
    add_test(NAME ai_service COMMAND rkmon_ai_tests)
    set_tests_properties(ai_service PROPERTIES TIMEOUT 15)
endif()

add_executable(rkmon_mqtt_tests mqtt_tests.cpp)
target_link_libraries(rkmon_mqtt_tests PRIVATE rkmon::mqtt)
target_include_directories(rkmon_mqtt_tests PRIVATE "${PROJECT_SOURCE_DIR}/src")
if(NOT CMAKE_CROSSCOMPILING)
    add_test(NAME mqtt_status COMMAND rkmon_mqtt_tests)
    set_tests_properties(mqtt_status PROPERTIES TIMEOUT 15)
endif()
