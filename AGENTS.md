# 仓库开发约定

项目模块职责、代码入口、架构和验证边界见 [docs/README.md](docs/README.md)。本文集中保存整个仓库适用的开发约束；具体用户操作和系统设计不要写进 AGENTS.md。

## C++ 源码

- 使用 C++17，保持编译器语言扩展关闭。
- 线程使用 `std::thread`、显式停止和 `join`，不使用 `std::jthread`、`std::stop_token` 等更新标准接口。
- `src/` 仅存放正式运行所需的应用组装和模块实现；测试、模拟服务、探针和演示放在 `tests/` 或 `examples/`。
- 不为尚未实现的业务添加空服务或模拟功能。
- 正式代码不保留演示示例、测试说明、占位教学或历史修改说明类注释；保留必要的接口契约、资源所有权、并发和异常处理原因。

## 脚本

- 新增或修改 `script/` 脚本时，shebang 后必须提供中文使用说明：用途、调用示例、参数/环境变量、运行前提、输出位置和重要副作用。
- 脚本应支持 `--help`，并能从任意工作目录通过自身路径定位项目根目录。
- 不在脚本中保存密码；构建使用 CMake + Unix Makefiles / GNU Make。
- 修改后验证 Bash/Python 语法和相关行为；同步类脚本优先使用试运行或本地模拟验证。
- PC 部署/更新统一使用 `script/deploy.sh`，复用 `build.sh`、`package-monitor.sh` 和板端 `install-monitor.sh`。程序产物、配置、静态资源、安装路径或服务依赖变化时，必须同步维护部署链路和 docs。

## 编辑与验证

- 保持修改聚焦，避免无关格式化和重构。
- 不提交构建目录、`.local`、Python `__pycache__`/`.pyc` 或验证产生的临时日志。
- 修改代码后先运行最窄的相关测试、编译或语法检查，再扩大验证范围。
- 不提交 Git commit，除非用户明确要求。
